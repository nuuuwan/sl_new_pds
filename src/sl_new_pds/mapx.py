def build_map(label_to_region_ids):
    print(label_to_region_ids)
    pass

if __name__ == '__main__':
    label_to_region_ids = {
        'Colombo': [
            'LK-11',
        ]
    }
    build_map(label_to_region_ids)
